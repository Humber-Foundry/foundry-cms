{
  "name": "foundry-cms",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @foundry/reference-site",
    "build:worker": "npm run cloudflare:build --workspace @foundry/reference-site",
    "deploy": "npm run deploy --workspace @foundry/reference-site",
    "dev": "npm run dev --workspace @foundry/reference-site",
    "preview": "npm run preview --workspace @foundry/reference-site",
    "provision:deployment-baseline": "npm run provision:deployment-baseline --workspace @foundry/reference-site",
    "generate:site-validator": "npm run generate:validator --workspace @foundry/site-definition",
    "graphify:query": "node scripts/graphify-index.mjs query",
    "graphify:refresh": "node scripts/graphify-index.mjs refresh",
    "graphify:status": "node scripts/graphify-index.mjs status",
    "test": "vitest run",
    "test:browser": "vitest run --config vitest.browser.config.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project apps/reference-site/tsconfig.json",
    "verify:integrity": "node scripts/verify-repository-integrity.mjs",
    "verify:mcp:conformance": "node scripts/run-mcp-conformance.mjs",
    "verify:mcp:inspector": "RUN_MCP_INSPECTOR=1 vitest run apps/reference-site/src/mcp-http-runtime.test.ts -t 'pinned official MCP Inspector'",
    "verify:site-validator": "npm run generate:validator --workspace @foundry/site-definition -- --check",
    "verify:bundle": "node scripts/verify-public-bundle.mjs",
    "verify:worker": "node scripts/verify-worker-runtime.mjs"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "packageManager": "npm@11.12.1",
  "devDependencies": {
    "@hyperjump/json-schema": "1.17.8",
    "@modelcontextprotocol/inspector": "2.1.0",
    "@types/commonmark": "0.27.10",
    "@types/node": "26.1.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "@vitest/browser": "4.1.10",
    "@vitest/browser-playwright": "4.1.10",
    "ajv": "8.20.0",
    "commonmark": "0.31.2",
    "playwright": "1.56.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
